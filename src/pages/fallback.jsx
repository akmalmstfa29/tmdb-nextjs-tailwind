import React from 'react';

const fallback = () => (
  <div className='grow flex justify-center items-center'>
    <span className='text-white font-bold'>Dude, Your are is offline!</span>
  </div>
);

export default fallback;